spring:
  application:
    name: evento

  datasource:
    url: jdbc:postgresql://localhost:5440/evento
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true

  flyway:
    enabled: true
    location: classpath:db/migration
